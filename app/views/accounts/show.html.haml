- @title = 'Home'

.col.col-sm-6
  - if can? :create, List
    .pull-right
      = link_to(new_list_path, class: 'text-success') do
        %span.glyphicon.glyphicon-plus
        New list

  - if @lists.present?
    %h2 Lists
    %ul
      -@lists.each do |list|
        - if @admin_lists.include?(list)
          %li
            %em= link_to list, list
        -else
          %li= link_to list, list

    - if @admin_lists.any?
      .text-muted
        Lists you are administrating are
        %em emphasized

