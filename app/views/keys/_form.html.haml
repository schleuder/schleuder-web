= form_tag(list_key_create_path(@list), method: 'post',  multipart: true, id: :keyupload) do
  .form-group
    = text_area_tag 'ascii', '', style: 'width: 100%; min-height: 15em;', placeholder: "Paste as text here&hellip;".html_safe

  .form-group
    %span.text-muted Or select a file:
    = file_field_tag :keyfile

  = submit_tag :Upload, class: 'btn btn-success'

