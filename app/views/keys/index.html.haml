.page-header
  %h1 Keys for #{@list}

%table.table
  %thead
    %tr
      %th Fingerprint
      %th Primary UID
  %tbody
    - @keys.each do |key|
      %tr 
        %td= link_to key.fingerprint, list_key_path(@list, key)
        %td= key.uids.first.uid
