#!/bin/sh

case "$1" in
  ""|-h|--help)
    echo "Usage: $(basename $0) list@hostname initadmin@example.org [/path/to/initadminkey]"
    exit 1
    ;;
esac

listname="$1"
adminaddr="$2"
adminkeypath="$3"


rails runner "SchleuderListCreator.create('$listname', '$adminaddr', '$adminkeypath')"
